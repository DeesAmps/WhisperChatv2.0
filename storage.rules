rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /profileImages/{userId} {
      // Only the owner may read/write their avatar
      allow read, write: if request.auth != null
                        && request.auth.uid == userId;
    }
  }
}
